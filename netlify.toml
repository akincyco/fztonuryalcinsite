[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — tüm yollar index.html'e (react-router istemci tarafı yönlendirme)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
